import AllCards from '../../achievements/jack-of-all-cards'
import {Background} from '../types'

const OldMenuBackground: Background = {
	type: 'background',
	id: 'old_menu',
	name: 'Old Menu',
	requires: AllCards.id,
}

export default OldMenuBackground
